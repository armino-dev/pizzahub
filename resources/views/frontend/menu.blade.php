<div class="row">
    @if($categories)
        <ul class="food-menu">
            @foreach($categories as $category)
                <li>
                    
                </li>
            @endforeach
        </ul>
        <div class="shopping-basket">

        </div>
    @endif
</div>